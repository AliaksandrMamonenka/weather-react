import React from 'react';

import './style.less';

class Header extends React.Component {
    render() {
        return (
            <h1 className="header_title">Weather checker</h1>
        );
    };
}

export default Header;